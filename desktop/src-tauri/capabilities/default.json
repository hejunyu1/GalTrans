{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum permissions for the GalTrans player window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
